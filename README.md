# NewsChatbot
This chatbot do following stuff:
* For given urls processed them using the langchain libraries
* Gemini models are used
* After processed the urls, any questions related to the urls can be answered with the links


### To run app
- add .env file and add the any llm api key
- run the requirement.txt with python to install
- then run command: streamlit run main.py
