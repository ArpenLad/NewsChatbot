# NewsChatbot
This chatbot does the following:
* For given URLs processed them using the langchain libraries
* Gemini models are used
* After processing the URLs, any questions related to the URLs can be answered with the links


### To run the  app
- add .env file and add any llm api key
- run the requirement.txt with Python to install
- then run command: streamlit run main.py
